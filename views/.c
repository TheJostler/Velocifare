
#include <unistd.h>
#include "../app/headers/network.h"
#include "../app/headers/kernel.h"

void render_index0html(int soc) {
	char *page[] = {
	
	};
for (int l=0; l<(sizeof(page)/sizeof(page[0])); l++) {
	put(page[l], peer);
}
}
